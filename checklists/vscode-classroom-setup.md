# � Настройка VS Code в учебном классе Windows 11

## Проблема
В учебных классах под управлением Windows 11 VS Code часто настроен на предыдущих пользователей. Это приводит к проблемам с Git-аутентификацией, настройками пользователя и синхронизацией с вашим личным GitHub аккаунтом.

## ⚠️ Основные признаки проблемы:
- При выполнении `git push` появляется ошибка аутентификации
- В VS Code открыт чужой GitHub аккаунт
- Коммиты отображаются от имени другого пользователя
- Синхронизация настроек работает неправильно
- Terminal показывает неправильные учетные данные

---

## 🔧 Пошаговое решение

### ШАГ 1: Сброс Git конфигурации в PowerShell

1. Откройте PowerShell в VS Code (`Ctrl + ` ` или `View → Terminal`)

2. **Очистите глобальную конфигурацию Git:**
```powershell
git config --global --unset user.name
git config --global --unset user.email
git config --global --unset credential.helper
```

3. **Установите свои данные:**
```powershell
git config --global user.name "Ваше Имя Фамилия"
git config --global user.email "ваша@почта.com"
```

4. **Проверьте настройки:**
```powershell
git config --global --list
```

### ШАГ 2: Очистка сохраненных учетных данных Windows 11

**PowerShell команды для Windows 11:**
```powershell
# Очистка Windows Credential Manager
git config --global --unset credential.helper
cmdkey /list | findstr git
# Если есть записи git - удалите их:
cmdkey /delete:git:https://github.com
```

**Альтернативный способ через GUI Windows 11:**
1. Нажмите `Win + R`, введите `control userpasswords2`
2. Вкладка "Advanced" → "Manage Passwords"
3. Найдите и удалите записи связанные с GitHub

### ШАГ 3: Сброс VS Code к исходному состоянию

1. **Выйдите из всех аккаунтов в VS Code:**
   - `Ctrl + Shift + P` → `GitHub: Sign Out`
   - `Ctrl + Shift + P` → `Azure: Sign Out`
   - `Ctrl + Shift + P` → `Microsoft: Sign Out`

2. **Очистите пользовательские настройки (осторожно в учебном классе):**
   ```powershell
   # Закройте VS Code полностью, затем:
   # НЕ УДАЛЯЙТЕ ПОЛНОСТЬЮ - только войдите в режим инкогнито
   code --disable-user-env-vars --disable-extensions
   ```

3. **Запустите VS Code в "чистом" режиме для текущего проекта:**
   ```powershell
   code --user-data-dir "$env:TEMP\vscode-temp" .
   ```

### ШАГ 4: Настройка для вашего проекта

1. **В открывшемся "чистом" VS Code войдите в свой GitHub:**
   - `Ctrl + Shift + P` → `GitHub: Sign In`
   - Следуйте инструкциям браузера
   - Авторизуйте VS Code

2. **Проверьте интеграцию с Git:**
   - Откройте Source Control (`Ctrl + Shift + G`)
   - Должен отображаться ваш репозиторий
   - Попробуйте сделать коммит

### ШАГ 5: Тестирование в PowerShell

1. **Создайте тестовый файл:**
```powershell
$gitUser = git config user.name
"# Тест от $gitUser" > test.md
git add test.md
git commit -m "Test: проверка настроек $gitUser"
git push origin main
```

2. **Проверьте на GitHub:**
   - Коммит должен отображаться от вашего имени
   - Email должен совпадать с вашим

---

## 🚨 Решение частых проблем

### Проблема: "Authentication failed"

**Решение:**
```bash
# Полная очистка credentials
git config --global --unset credential.helper
git config --global credential.helper manager-core

# Принудительная повторная аутентификация
git push origin main
# Откроется окно для ввода логина/пароля или токена
```

### Проблема: "Permission denied (publickey)"

**Решение:**
1. Используйте HTTPS вместо SSH:
```bash
git remote set-url origin https://github.com/username/repository.git
```

2. Или настройте SSH ключ (для продвинутых):
```bash
ssh-keygen -t rsa -b 4096 -C "ваша@почта.com"
# Добавьте публичный ключ в GitHub Settings → SSH Keys
```

### Проблема: "Repository not found"

**Решение:**
```bash
# Проверьте URL репозитория
git remote -v

# Если неправильный - исправьте:
git remote set-url origin https://github.com/ВАШ_USERNAME/UP05EDU001.git
```

### Проблема: VS Code показывает чужие настройки

**Решение:**
```bash
# Запуск VS Code с временными настройками только для этого проекта
code --user-data-dir "%TEMP%\vscode-student-$(whoami)" .
```

---

## 🎯 Быстрый checklist для студентов

**Перед началом работы (5 минут):**
- [ ] `git config --global user.name "Ваше Имя"`
- [ ] `git config --global user.email "ваша@почта"`
- [ ] `git config --global --list` - проверили что данные ваши
- [ ] Вышли из чужих аккаунтов в VS Code
- [ ] Вошли в свой GitHub в VS Code
- [ ] Сделали тестовый коммит
- [ ] Проверили на GitHub что коммит от вашего имени

**В конце работы (2 минуты):**
- [ ] Сделали финальный push
- [ ] Вышли из своего GitHub аккаунта
- [ ] Очистили git credentials: `git config --global --unset user.name`

---

## 💡 Советы для работы в классе

### Используйте портабельные настройки Windows 11:
```powershell
# Создайте папку с настройками проекта
mkdir .vscode-settings
code --user-data-dir .vscode-settings .
```

### Работайте через браузер (запасной вариант):
- Откройте github.com
- Нажмите `.` (точку) в репозитории  
- Откроется github.dev - онлайн VS Code
- Все изменения автоматически синхронизируются

### Используйте PowerShell вместо Git Bash:
```powershell
# PowerShell более предсказуем в Windows 11
git config --global user.name "Ваше Имя"
git config --global user.email "ваша@почта"
```

---

## 🆘 Если ничего не помогает

### План Б: Работа через веб-интерфейс
1. Откройте свой репозиторий на github.com
2. Нажмите `Add file` → `Create new file` или `Upload files`
3. Редактируйте файлы прямо в браузере
4. Коммитьте через веб-интерфейс

### План В: Скачивание и загрузка
1. `Code` → `Download ZIP` - скачайте репозиторий
2. Работайте локально
3. `Upload files` - загрузите измененные файлы через веб

### Обращение за помощью:
**При обращении к DeepSeek включите информацию о среде:**
```
Я студент, работаю в учебном классе на Windows 11. VS Code и Git настроены на предыдущего пользователя. Использую PowerShell. Вот вывод команд:

git config --global --list
```
<вставьте вывод>
```

git remote -v  
```
<вставьте вывод>
```

Помогите настроить для моего GitHub аккаунта.
```

---

**Помните:** В учебном классе Windows 11 главное - получить рабочий результат. Если настройка занимает больше 10 минут, используйте веб-интерфейс GitHub!

---

*Обновлено для УП.05 День 1 - Windows 11 + PowerShell*
